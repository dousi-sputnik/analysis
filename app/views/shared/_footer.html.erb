<div class="container">
  <footer class="py-3 my-4">
    <ul class="nav justify-content-center border-bottom pb-3 mb-3">
      <li class="nav-item"><%= link_to "Home", root_path, class: "nav-link px-2 text-body-secondary" %></li>
      <% if user_signed_in? %>
        <li class="nav-item"><%= link_to "一覧", user_items_path(current_user), class: "nav-link px-2 text-body-secondary" %></li>
        <% unless current_user.guest? %>
          <li class="nav-item"><%= link_to "Profile", user_path(current_user), class: "nav-link px-2 text-body-secondary" %></li>
        <% end %>
      <% end %>
      <li class="nav-item"><%= link_to "プライバシーポリシー", policy_path, class: "nav-link px-2 text-body-secondary" %></li>
      <li class="nav-item"><a href="#" class="nav-link px-2 text-body-secondary">About</a></li>
    </ul>
    <p class="text-center text-body-secondary"><%= Time.now.year %> ABCanalysis </p>
  </footer>
</div>
